!!!! NEVER PUSH .env file !!!!

To add [ global ]:
1. time of response for ob with rest method time = "response.elapsed.total_seconds()"


PostgreSQL :

tables desc:-----------------------------

zonda_ob --> example rate collection for bot using websocket, it won't be stored into database because of its pace

zonda_rest_ob --> example orderbooks collection using rest api and async function

zonda_socket_trades --> the optimal database with socket connection to last trades, it follows data without looking at the pace
so it can stay in websocket mode without any threat do database size